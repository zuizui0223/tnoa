name: validate-paper-package

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Validate TNOA paper manifest
        run: python scripts/validate_paper_manifest.py
      - name: Audit manuscript claim boundaries
        run: python scripts/audit_manuscript_claims.py
      - name: Check MEE submission package
        run: python scripts/check_mee_submission.py
      - name: Assemble anonymised MEE main text
        run: python scripts/assemble_mee_main.py --output /tmp/TNOA_P1_MEE_MAIN.md
